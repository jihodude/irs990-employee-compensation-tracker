def assign_standard_position(irs_title):
    """
    Returns the IRS title as-is for the standard_position column.

    Title classification requires human judgement and is intentionally left
    for manual review in Excel after the CSV is generated.
    """
    return irs_title
